while True:
    try:
        fraction = input("Fraction: ")
        x = int(fraction[0])
        y = int(fraction[2])
        if x > y:
            except 
        elif y == 0:
            raise ZeroDivisionError

        percent = int(x / y * 100)
        break
    except ValueError:
        if x > y:
            pass
    except :
        pass

print(f"{percent}%")
